<div class="container">
    <h2>配送先登録/一覧</h2>
  <div class="row">
    <%= form_with model: @address, url: addresses_path do |f| %>
      <label>郵便番号（ハイフンなし）</label>
      <%= f.number_field :postal_code %></br>
      <label>住所</label>
      <%= f.text_field :address %></br>
      <label>宛名</label>
      <%= f.text_field :name %>
      <%= f.submit "新規登録", class: "btn btn-success" %>
    <% end %>
  </div>

  <div class="row">
    <table class="table">
      <thead>
        <tr>
          <td>郵便番号</td>
          <td>住所</td>
          <td>宛名</td>
          <td></td>
        </tr>
      </thead>
      <tbody>
        <% @addresses.each do |address| %>
        <tr>
          <td>
            <%= address.postal_code %>
          </td>
          <td>
            <%= address.address %>
          </td>
          <td>
            <%= address.name %>
          </td>
          <td>
            <%= address.submit "編集する", class: "btn btn-success" %>
            <%= address.submit "削除する", class: "btn btn-danger" %>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>